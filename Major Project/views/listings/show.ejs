<% layout("/layouts/boilerplate") %>

<div class="row">
  <div class="col-8 offset-2">
    <h3>Listing Details :</h3>

    <div class="card show-card">
      <img
        src="<%=listing.image.url%>"
        class="card-img-top show-img"
        alt="listing_image"
         onerror="this.style.visibility='hidden'"
      />
      <div class="card-body">
        <h5 class="card-title"><%= listing.title%></h5>
        <p class="card-text mb-2"><%= listing.description%></p>

        <p class="card-text mb-2">
          <b>&#8377; <%= listing.price.toLocaleString("en-IN")%></b>
        </p>
        <p class="card-text mb-2"><%= listing.location%></p>
        <p class="card-text mb-3"><%= listing.country%></p>

        <div class="btns mt-3">
          <a
            href="/listings/<%= listing._id %>/edit"
            class="btn btn-dark edit-btn col-1"
            >Edit</a
          >
          <form
            action="/listings/<%=listing._id%>?_method=DELETE"
            method="POST"
            class="d-inline offset-1"
          >
            <button class="btn btn-dark">Delete</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
